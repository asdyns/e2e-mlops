apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ml-system

resources:
  - namespace.yaml
  - configmap.yaml
  - storage-class.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml

commonLabels:
  app: house-price-api
  managed-by: kustomize

